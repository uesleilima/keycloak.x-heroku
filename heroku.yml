setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - /opt/jboss/tools/docker-entrypoint.sh --auto-config

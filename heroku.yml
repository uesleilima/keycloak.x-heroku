setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE
build:
  docker:
    web: Dockerfile
    worker: Dockerfile-release
release:
  image: worker
  command:
    - /opt/jboss/tools/deployment.sh
